[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Disable healthcheck temporarily to see actual logs
healthcheckPath = ""
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
